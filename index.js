// Load the DOM content.
document.addEventListener("DOMContentLoaded", () => {
    // Change the text on the page with an ID of text.
  document.getElementById('text'). textContent = 'This is really cool!';
})